<h2><%= @artist.name %>'s Song List: </h2>
<ul>
  <% @artist.songs.each do |song| %>
    <li><a href="/songs/<%= song.slug %>"><%= song.name %></a> -
      <% song.genres.each do |genre|%>
        <a href="/genres/<%= genre.slug %>"><%= "[#{genre.name}]"%> </a>
      <% end %>
    </li>
  <% end %>
</ul>

<a href="/">Home</a>
<a href="/artists">Artist List</a>
