<h2><%= @genre.name %>'s Song List: </h2>
<ul>
  <% @genre.songs.each do |song| %>
    <li><a href="/songs/<%= song.slug %>"><%= song.name %></a> - <a href="/artists/<%= song.artist.slug %>"><%= song.artist.name %></a></li>
  <% end %>
</ul>

<a href="/">Home</a>
<a href="/genres">Genre List</a>
